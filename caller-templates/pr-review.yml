name: PR Review
on:
  pull_request_target:
    types: [opened, reopened, ready_for_review, synchronize]
jobs:
  pr-review:
    uses: zyzhou-s-lab/repo-template/.github/workflows/reusable-pr-review.yml@v1
    secrets: inherit
