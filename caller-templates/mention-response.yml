name: Mention Response
on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
jobs:
  mention-response:
    uses: zyzhou-s-lab/repo-template/.github/workflows/reusable-mention-response.yml@v1
    secrets: inherit
